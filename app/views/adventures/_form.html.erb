<%= form_with(model: adventure, local: true) do |form| %>
  <% if adventure.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(adventure.errors.count, "error") %> prohibited this adventure from being saved:</h2>

      <ul>
        <% adventure.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :current_chapter %>
    <%= form.text_field :current_chapter %>
  </div>

  <div class="field">
    <%= form.label :load_code %>
    <%= form.text_field :load_code %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="field">
    <%= form.label :test %>
    <%= form.check_box :test %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
