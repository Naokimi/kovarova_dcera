<%= form_with(model: non_player_character, local: true) do |form| %>
  <% if non_player_character.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(non_player_character.errors.count, "error") %> prohibited this non_player_character from being saved:</h2>

      <ul>
        <% non_player_character.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :hp_left %>
    <%= form.number_field :hp_left %>
  </div>

  <div class="field">
    <%= form.label :adventure_id %>
    <%= form.text_field :adventure_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
